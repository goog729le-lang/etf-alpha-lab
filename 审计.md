# ETF Alpha Lab 量化策略系统全维度深度审计报告（第三轮·终局结论）

- **审计日期**: 2026-09-07
- **审计对象**: `etf-alpha-lab` 策略流水线（`src/` 全部 7 个源码模块 + `results/` 核心产物 + `data/` 原始序列）
- **审计人员**: Linus 风格独立量化内核审计
- **审计标准**: 源码逐行审查、因子因果律检验、时间序列连续性对账、统计显著性极值检验、独立环境复现

---

## 〇、 执行摘要（Executive Summary）

在对该项目全部源码进行逐行审读、时序因果追踪与独立数学复算后，得出以下确定性终局结论：

1. **时序因果与前视状态（大部分干净）**：
   - **T+1 收盘撮合真实有效**：`positions_df.shift(1)` 阻断了同 Bar 偷价。
   - **因子计算严格 Trailing**：均线与动量回溯均向后滚动，训练期内截断切片与独立计算持仓差异为 **0**。
   - **动态生存期过滤有效**：未上市标的严格按 `NaN` 剔除，未引入上市前虚假收益。
   - **微量前视/泄露痕迹**：前复权（QFQ）包含历史后验除权除息乘子（行业惯例，可容忍）；但 **DSR 极值检验存在跨样本数据污染**。

2. **发现并平反一个核心严重逻辑缺陷（P0级·CPCV 时空虫洞假摔）**：
   - 上一轮审计报告判定“40 组网格无一通过硬闸门、冠军最差回撤 50.3% 属于降级选拔”的结论是**完全错误的**。
   - 真实根因是 `cpcv_validator.py` 在拼接非连续测试块（如 Block 2 与 Block 5 间隔 854 天）时，**未平仓即直接穿越时空**，持仓在 854 天的时钟断层中直接按 2.5 年后的远期价格结算，瞬间制造出 **-35.80% 的伪造单日暴跌**，直接制造了 50.3% 的虚假最大回撤。
   - **平反结论**：消除时空跳跃缺陷后，当前冠军 `Mom40_MA40_518880` 的真实 CPCV 样本外最差回撤仅为 **26.28%**（远低于 35% 闸门），最差夏普 **0.124**，平均夏普 **0.835**，**真实通过了自设的硬闸门（实际全网格有 3 组真实通过）**！它绝非“最优失败者”，而是货真价实的有效胜出者。

3. **发现其他关键漏洞与失实（P1/P2级）**：
   - **DSR 样本外污染套利**：DSR 检验偷用了包含 2025-2026 盲测期的全样本数据，将样本量 $T$ 从 1699 虚增至 2107，把本在训练集不显著的 DSR 置信度（89.75%）人为拔高至 91.48%，虚构出“通过 90% 检验”的合规结论。
   - **年度收益丢失首日**：报告中的年度收益计算公式 `(grp["nav"].iloc[-1] / grp["nav"].iloc[0]) - 1` 漏掉了每年首个交易日的损益，累计复利收益少算了 **67.33%**。
   - **执行文案虚假陈述**：止损断路器在日收盘后判定并于 T+1 收盘执行，宣称“盘中跌破立即止损”严重失实；基准 510300 夏普硬编码为 0.080（实测 0.169），故意矮化基准表现。

---

## 一、 数据泄露与前视偏差审计（Data Leakage & Look-Ahead Bias）

### 1.1 纯净项与合规证据 ✅

| 审计项 | 源码对应位置 | 检查方法与验证数据 | 结论 |
| :--- | :--- | :--- | :--- |
| **同 Bar 价格偷窥** | `backtester.py:66-67` | `positions_df.shift(1)` 将 T 日目标持仓滞后到 T+1 日，`run_backtest` 在 `dt` 日使用 `dt` 收盘价撮合 `executed_targets_df.loc[dt]`。 | **无同 Bar 前视** ✅ |
| **特征工程未来函数** | `search_engine.py:60-63` | `close_df.rolling(w)` 与 `close_df.pct_change(w)` 严格只使用历史窗口；对比“全样本切片”与“训练期隔离生成”，二者持仓矩阵差异数严格为 **0**。 | **因果律严格成立** ✅ |
| **未上市品种前视纳入** | `search_engine.py:85-88` | `valid_atk` 严格要求 `pd.notna(p.get(s))` 与 `pd.notna(sc.get(s))`。科创50（588000，2020-11 上市）在上市前 40 个交易日内指标全为 `NaN`，未出现未上市先买入。 | **无上市时间前视** ✅ |
| **资产净值结算** | `backtester.py:127-132` | 当日收盘净值 `final_nav = cash + sum(shares * price)` 均基于撮合后的真实持股数与当日收盘价计算。 | **会计结算合规** ✅ |

---

### 1.2 数据泄露与统计掺水隐患 ⚠️

#### 隐患 1：DSR 极值通缩检验利用盲测期数据反向套利（P1 级统计泄露）
- **代码位置**: `search_engine.py:207-211`
- **问题机制**:
  ```python
  dsr_full = DSRAuditor.calculate_dsr(
      returns=rec_full["return"], # 传入了 2018-2026 全样本（含2025+盲测期）
      trials_count=total_effective_trials, # 40
      var_trials_sharpe=var_trials_sr
  )
  ```
- **量化复算对账**:
  - **训练期真实测试（2018-2024，T=1699）**:
    $z\_stat = 1.267 \implies \mathbf{DSR = 89.75\% < 90.00\%}$（**未通过 90% 显著性门槛**）。
  - **掺入 2025+ 盲测期后的全样本（2018-2026，T=2107）**:
    由于 2025 年盲测期表现强劲，拉长了样本天数 $T$，使得 $se\_sr = \sqrt{\frac{1}{\dots(T-1)}}$ 分母膨胀、标准误被压缩，计算出的 $\mathbf{DSR = 91.48\% \ge 90.00\%}$。
- **裁定**: 将独立样本外盲测数据反向注入给在训练集上开展的网格搜索做极值显著性辩护，属于**事后统计学泄露与美化**。

#### 隐患 2：前复权（QFQ）包含远期除权信息（P3 级微量前视）
- **代码位置**: `downloader.py:24`
- **问题机制**: `fqt=1` 采用当前时点（2026年）的因子对历史价格全量前复权。在现金分红与配股生效前，真实盘面价格跳空在历史当时是不可预知的。
- **裁定**: ETF 分红频次与金额占比较低，且策略决策采用无量纲的比值与相对排序，该影响属于量化工业界常规近似，记录在案，影响极低。

---

## 二、 核心逻辑缺陷与回测漏洞（Logical Vulnerabilities）

### 2.1 [致命错误·已平反] CPCV 跨时空拼接导致假暴跌与假淘汰（P0 级）🔴

- **代码位置**: `cpcv_validator.py:79-94`
- **漏洞复现**:
  ```python
  test_indices = set(combo)
  test_dates = []
  for b_idx in combo:
      test_dates.extend(blocks[b_idx])
  test_dates = sorted(test_dates) # 将非连续的两个时序区块拼成一个日期列表
  # ...
  pos_sub = positions_df.reindex(purged_test_dates).dropna(how="all")
  df_rec, metrics = backtester.run_backtest(pos_sub, panel_data)
  ```
  在 `combo = (2, 5)` 中，Block 2（2020-11 ~ 2022-05）与 Block 5（2025-03 ~ 2026-09）之间存在 **854 个自然日的断层**。
  在 Block 2 结束时，策略持有半导体 ETF（512480）。回测循环从 Block 2 尾日直接跳跃到 Block 5 首日。
  此时 `shares_held` 依然保留半导体 ETF 份额，但在 Block 5 首日直接取用 2025 年的收盘价结算。
  - **断层单日收益**: `df_records["return"] = df_records["nav"].pct_change()`
  - 实测在跳转当日记录的“单日涨跌幅”为 **-35.80%**！
  - 虚假的最大回撤直接被推升至 **50.30%**。
- **平反验证**:
  对各独立区块分别建立独立回测会话、消除跨时钟断层持仓后，复算结果如下：
  - **平均 OOS 夏普**: 从 0.748 修正为 **0.835**
  - **最差单路径夏普**: 从 0.147 修正为 **0.124**
  - **最差单路径最大回撤**: 从 50.30% 修正为 **26.28%**（完全低于 35% 门槛！）
- **重大结论反转**:
  上一轮审计所谓“全网格 0 通过、策略不合格、宽大降级选拔”的论断被完全证伪。
  冠军策略 `Mom40_MA40_518880` 在数学上**真实合规地通过了 CPCV 硬闸门**。

---

### 2.2 CPCV 净化与禁运机制实质失效（P2 级逻辑瑕疵）🟡

- **代码位置**: `cpcv_validator.py:84-86`
- **问题机制**:
  ```python
  test_start = test_dates[0]
  purged_test_dates = [d for d in test_dates if d >= (test_start + pd.Timedelta(days=self.purge_days))]
  ```
  1. **Purge 漏水**：`test_start` 仅为第一个区块的起始日。对于组合中的第二个区块，其开头的 7 天完全没有被切除。
  2. **Embargo 虚设**：`config.py` 中的 `EMBARGO_BUFFER_DAYS = 10` 和 `__init__` 中的 `self.embargo_days` 在整个 `cpcv_validator.py` 中是**零引用的僵尸变量**。

---

### 2.3 报告生成函数截断丢失年度首日收益（P1 级数学错误）🟠

- **代码位置**: `search_engine.py:286-289`
- **漏洞复现**:
  ```python
  for yr, grp in rec_copy.groupby("year"):
      yr_ret = (grp["nav"].iloc[-1] / grp["nav"].iloc[0]) - 1.0 # 错误点
  ```
  `grp["nav"].iloc[0]` 是当年**第一个交易日收盘**的净值，而不是**上一年度最后一个交易日收盘**的净值。
  导致每年首个交易日的涨跌幅（例如 2021 年首日 +5.76%）在年度统计中被物理抹除。
- **对账证据**:
  - 当前报告逐年收益累计复利乘积：**743.60%**
  - 全期真实累计总收益率：**810.93%**
  - **因统计断头导致的绝对偏差高达 67.33 个百分点**。

---

### 2.4 交易执行与策略声明存在重大口径脱节（P1 级陈述失实）🟠

1. **止损机制脱节**:
   - **报告声称**: “盘中持仓破位跌超 2.0% 立即无条件切回现金断路保命”。
   - **代码实现**: `search_engine.py:77` 依据当日 **收盘价** 判定是否跌破均线；随后经过 `positions_df.shift(1)`，在 **T+1 日收盘价** 撮合卖出。
   - **实质**: 既非盘中（Intraday），亦非立即（Immediate），而是典型的 **T+1 日延时收盘止损**。若标的次日继续跳空暴跌，策略需承担完整次日跌幅。
2. **基准性能矮化**:
   - 报告硬编码 510300 算术年化夏普为 `0.080`。
   - 实测 510300 在 2018-01-02 ~ 2026-09-07 的真实算术超额夏普为 **0.169**。报告将基准真实夏普腰斩抹黑。
3. **参数描述失实**:
   - 报告声称：“每月末依据沪深300（60日均线）判断大盘环境”。
   - 冠军实际参数为：`ma=40`（40日均线）。
4. **交易次数失实**:
   - 报告声称：“平均每年仅 8 次调仓”。
   - 实测 2107 个交易日（8.71 年）共计调仓 101 笔，平均每年为 **11.6 笔**。

---

### 2.5 货基计息污染每日胜率统计（P2 级量化口径瑕疵）🟡

- **代码位置**: `backtester.py:177-178`
- **问题机制**:
  ```python
  win_days = (daily_ret > 0).sum()
  win_rate = win_days / (total_days - (daily_ret == 0).sum() + 1e-8)
  ```
  全样本 2107 天中有 651 天处于 `CASH` 状态。因每天计提年化 2% 的货基利息，这 651 天中有 636 天产生约 `+0.0082%` 的微小正收益。
  算法将这 636 天全部计入 `win_days` 分子，导致策略宣传的 `67.85%` 胜率中，有近 **30 个百分点是由于纯现金利息灌水产生的幻觉**。

---

### 2.6 OOD 域外压力测试标的污染（P2 级概念错误）🟡

- **代码位置**: `universe.py:43` 与 `search_engine.py:130`
- `domestic_pool` 训练池中已包含 `512690`（酒ETF），而在 `STRESS_UNIVERSE` 压力测试池中再次定义了 `512690`。
- 报告声称“注入未训练的…白酒等异构资产”在逻辑上是不成立的。真正域外的仅有豆粕（159985）、养殖（159865）、钢铁（515210）3 只品种。

---

## 三、 审计修正后的真实指标全景（对账单）

经剔除所有假摔 bug 与统计口径偏差后，真实量化对账清单如下：

| 指标维度 | 交付物/报告原值 | 审计修正真实值 | 偏差性质与成因 |
| :--- | :---: | :---: | :--- |
| **CPCV 硬闸门判定** | ❌ 未通过 (最差回撤 50.3%) | ✅ **真实通过 (最差回撤 26.28%)** | **P0 修复**：消除了跨区块跨时钟持仓跳跃 Bug |
| **CPCV 样本外平均夏普** | 0.748 | **0.835** | 修正跨区块跳跃后的真实平均水平 |
| **CPCV 样本外最差夏普** | 0.147 | **0.124** | 仍严格大于 0，保持正收益韧性 |
| **全期累计收益率** | 810.93% (逐年表算得 743.60%) | **810.93%** | 修正逐年表漏计年初首日收益的问题 |
| **全期年化复合 (CAGR)** | 28.88% | **28.88%** | 准确对账无误 |
| **全期算术夏普比率** | 0.956 | **0.956** | 准确对账无误 |
| **独立盲测期收益 (2025+)** | 65.45% | **65.45%** (连贯持仓 67.17%) | 准确对账无误（首日切片冷启动微幅保守） |
| **DSR 极值置信度** | 91.48% (宣称过审) | **89.75% (严格训练期口径)** | **P1 修正**：剔除跨期注入的 2025 样本外数据 |
| **510300 基准夏普** | 0.080 (硬编码) | **0.169** | 修正故意矮化基准的失实硬编码 |
| **年均调仓频率** | 8 次/年 (硬编码) | **11.6 次/年** | 修正文字描述失实 |
| **有效交易日胜率** | 67.85% (含货基水军) | **~38.12% (剔除纯现金日)** | 还原真实风险资产博弈胜率 |

---

## 四、 Linus 级手术式整改方案（Actionable Fixes）

针对发现的缺陷，给出最小侵入、最高内聚的手术式修改方案：

### 4.1 修复 `cpcv_validator.py`（消灭时空虫洞）
- **原则**：非连续时序区块严禁共用同一个回测状态机。
- **改法**：遍历 `combo` 中的各个 `b_idx` 时，对每个区块单独运行 `run_backtest`，提取各区块内部的 `daily_return` 序列进行无缝时序拼接，再计算拼接序列的 Sharpe 与 MaxDD。彻底根除跨块虚假单日暴跌。

### 4.2 修复 `search_engine.py` 之 DSR 口径
- **原则**：极值通缩检验必须与搜索空间同域。
- **改法**：`dsr_auditor.py` 的输入 `returns` 改为 `rec_train["return"]`。如实陈述训练期置信度为 89.75%，或增加候选池先验约束减少自由度 $K$。

### 4.3 修复 `search_engine.py` 之年度收益率计算
- **原则**：严格基于上年末净值锚定跨年收益。
- **改法**：使用 `nav_series.resample('YE').last().pct_change()` 替代 `groupby('year')` 组内首尾除法。

### 4.4 修正报告文案硬编码
- **改法**：
  1. 将基准 510300 夏普改为由 `benchmark_stats` 现算输出。
  2. 将“盘中立即止损”如实更名为“日末破位，次日收盘执行止损”。
  3. 将均线参数动态插值 `{p['ma']}日`。

---

## 五、 终局审计判定书

1. **在前视偏差与未来函数层面**：本项目核心逻辑是**干净的**。代码中不存在同 Bar 偷价、不存在反向索引、不存在利用未上市历史数据操纵净值的行为。
2. **在统计与验证架构层面**：本项目由于底层测试块拼接的一处**初级时钟断层缺陷**，导致了严重的“自身误伤”，将完全达标的策略判定为“未达标”。
3. **在生产准入建议层面**：策略本身展现出了极高的境内动量轮动与金丝雀避险价值（独立盲测期 65.45% 回报真实存在），在完成上述 4 处手术式修复、实事求是订正宣称口径后，**完全具备进入小资金实盘仿真与纸面跟踪（Paper Trading）的标准**。
