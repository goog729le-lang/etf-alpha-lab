# ETF Alpha Lab 策略验证体系审计报告（第二轮）

- **审计日期**: 2026-09-07（第二轮，覆盖 16:37~16:57 重构后的当前版本）
- **审计对象**: 纯 A 股行业轮动策略流水线（`src/` 全部 7 个模块 + `results/` 三件产物）
- **审计方式**: 源码逐行审读 + 独立复算（冠军全样本回测、盲测期回测、逐笔撮合行为验证、报告数字逐项对账）
- **重要说明**: 第一轮审计针对 16:37 之前的旧版本（GEM 纳指/黄金轮动）。旧版全部产物已于 16:57 重新生成，本报告仅对当前版本负责；旧版结论以附录形式保留在文末。

---

## 〇、总体结论

**当前版本在前视/泄露层面是干净的**——T+1 撮合为真、信号层全部 trailing、2025+ 盲测期数据物理隔离且从未参与选拔、报告数字与代码产物逐日对账一致（独立复算零偏差）。第一轮的五个 P0 问题全部得到实质性修复。

**但该策略按项目自定的 CPCV 硬闸门是不合格的**：40 组网格候选**无一通过**（冠军最差路径回撤 50.3% > 35% 上限），当前冠军是"降级选拔"（best-of-failures）的产物，而报告没有给出这一总体判定。此外仍残留报告硬编码文案失实、CPCV 选拔与报告自引用、资产池幸存者偏差、OOD 测试设计缺陷等问题。

**一句话**：机器诚实了，但判决书还差最后一行——"本案无达标策略，冠军为宽大处理"。

---

## 一、前视与数据泄露检查（当前版本）

### 1.1 结论：未发现前视或泄露 ✅

以下每一条都做了代码审读 + 数据行为验证双重确认：

| 检查项 | 方法 | 结果 |
|---|---|---|
| 反向 shift / 序列反转 | 全代码扫描 | 无 `shift(-1)`、无 `[::-1]` |
| 特征计算方向 | 审读 + 复算 | 均线/动量/金丝雀全部 trailing；`pct_change(fill_method=None)` 已修复 pad 告警 |
| 同 bar 偷价 | 撮合行为验证 | `backtester.py:66-67` 用 `positions_df.shift(1)` 实现 T+1；实测 73 个换仓执行日中 57 个的前一日为月末决策日、16 个为破位止损日、**0 个无法解释**；买入日净值收益系统性低于新标的当日收益（均值 -0.68%，动量策略"涨后买入"的自然结果），证明买入未捕获当日收益 |
| 未上市标的前视 | 审读 | `generate_domestic_sector_positions` 对未上市标的 NaN 排除（`p.get(s)` + `pd.notna`），588000（2020-11 上市）等动态纳入，无未来数据 |
| 盲测期污染 | 代码结构审读 | 冠军选拔仅用 `pos_train`（≤2024-12-31）的 CPCV 与训练期指标（`search_engine.py:151-192`）；2025+ 数据仅用于终审、DSR、报告展示，且盲测期指标已独立复算一致 |
| 产物与代码一致性 | 独立复现 | 冠军全样本回测 5 项指标（final_nav/cagr/sharpe/mdd/trades）与 JSON 完全一致；CSV 逐日净值与复算偏差 0.000000；盲测期 4 项指标复算一致 |

### 1.2 残余的轻微乐观假设（P3）

1. **停牌日按 ffill 陈旧价格可成交**（`backtester.py:61,92-106`）：卖出守卫只检查 `pd.notna`，ffill 后恒为真，注释"停牌无法成交则保持原仓位"与实际行为不符。当前 15 只 ETF 停牌极少，影响可忽略。
2. **佣金最低 5 元未建模**：1 万元本金单笔佣金 0.0003×1 万 = 3 元 < 券商实收下限 5 元，101 笔交易累计低估成本约 200 元（约占本金 2%）。系统性轻微乐观。
3. **QFQ 复权序列以当前因子改写历史**：历史信号与当时实盘可见信号存在理论差异；当前策略全部使用比值/动量类信号且所选 ETF 分红极少，影响可忽略。行业惯例，记录在案。
4. **滑点固定 0.001 元/股**：对低价 ETF（588000 约 1 元，合 10bp）偏保守、对高价 ETF（512800 约 8 元，合 1.25bp）偏乐观，方向不一，整体中性。

---

## 二、验证体系与逻辑问题（当前版本）

### 2.1 全网格无一通过自定硬闸门，冠军为降级选拔，报告未给出总体判定（P1）🔴

**位置**: `search_engine.py:169-192`、`champion_strategy.json`、报告第四节

- 硬闸门：`min_mean_sharpe=0.50`、`min_worst_sharpe=0.0`、`max_worst_mdd=35.0`（`search_engine.py:121`）。
- 冠军（Mom40_MA40_518880）实测：mean_oos_sharpe 0.748 ✅、worst_oos_sharpe 0.147 ✅、**worst_oos_max_dd 50.3% ❌（> 35%）** → `is_cpcv_passed: false`。
- JSON 里 `is_cpcv_passed: false` 意味着 fallback 分支触发（`search_engine.py:177-183`），即 **40 组候选中 0 组通过闸门**，冠军是"最差路径夏普降序"下的最优失败者。
- 报告第四节如实展示了"❌ 未达 CPCV 严苛设定硬指标"（诚实，值得肯定），**但全文没有任何地方给出总体判定**："本项目当前无策略达到入库标准，交付冠军为降级选拔结果"。报告通篇以"最终研发交付报告 / 优胜策略"的口径呈现 28.88% CAGR，读者很容易把 ❌ 当成个别指标的小瑕疵而非整体不合格。

**修复建议**: 报告增加总判定区块：`is_cpcv_passed=false` 时明确输出"⚠️ 无候选策略通过硬闸门，本报告为降级选拔结果，不建议实盘入库"，并把该判定置于报告最顶部。

### 2.2 CPCV 路径同时用于选拔与报告，冠军 CPCV 统计量偏乐观（P1）🟠

**位置**: `search_engine.py:185-192`

无论通过闸门与否，选拔排序键都使用 `worst_oos_sharpe`（fallback 分支甚至直接按它降序）——即在 40 组候选里挑"15 条 CPCV 路径最差值最大"的那个，**然后**把同一批路径写进报告作为"样本外平均夏普 0.748 / 最差 0.147"。对被选中者而言，这批数字是 40 选 1 的极值，天然偏乐观。

**需要说明的边界**：该问题被限制在训练期（2018-2024）内部，2025+ 盲测期不受影响，因此**盲测期指标（Sharpe 0.931 / 回报 65.45% / 回撤 34.14%）是全报告唯一完全干净的样本外证据**——恰好它也足够强。

**修复建议**: 报告中 CPCV 区块标注"该统计量参与了选拔，存在选择偏差，以盲测期终审为准"；或对入选冠军用不同随机种子/块划分重跑一次 CPCV 作为确认。

### 2.3 资产池层面的幸存者/后视偏差仍是最大的水分杠杆（P1）🟠

**位置**: `search_engine.py:128-131`

- 11 只行业 ETF（半导体、通信、科创50、券商、银行、消费、酒、医药、军工、新能源、创业板）是 2026 年回望确定的现存主流品种，**池的选择本身利用了 2018-2026 的行情知识**（半导体/通信 2020 年、券商 2019/2020 年的行情事后可见）；
- 2020 年 +219.88%、2025 年 +88.88% 的收益高度依赖"池里恰好有当年最猛的行业"；
- DSR 的 `trials_count=40` 只计了参数网格（5 动量 × 4 均线 × 2 防守），**池构成本身的自由度没有计入**。

相对旧版（直接用纳指+黄金）已大幅改善，但未根除。**修复建议**: 池敏感性测试——随机剔除/替换 2~3 只行业后重跑网格，看冠军参数与收益的稳定性；或使用当前时点按规则（流动性、上市年限）可客观构造的池。

### 2.4 报告残留硬编码文案，且已核实为失实（P1）🟠

**位置**: `search_engine.py:305-384` 模板

报告标题声称"全部由实际运行流水现算对账"、逐年表声称"零手写"，但评价列与基准列仍有硬编码，其中三处经独立复算**确认失实**：

| 报告文案 | 实际复算 | 判定 |
|---|---|---|
| "净赚近 6 倍" | 期末 91,093.35 元 = **9.11 倍**（净赚 8.1 倍） | 失实 |
| "平均每年仅 8 次调仓" | 101 笔 / 8.71 年 = **11.6 笔/年** | 失实 |
| 基准夏普 "0.080" | 510300 算术夏普 = **0.169** | 失实（基准列未现算夏普） |
| "依据沪深300（60日均线）" | 金丝雀均线 = 冠军参数 `ma=40` | 失实 |
| "盘中持仓破位跌超 2.0% 立即切回现金" | 实现为**日收盘**破位判断 + **T+1 收盘**执行，非盘中、非立即 | 失实 |
| 基准卡玛 "0.075" | 3.37/44.75 ≈ 0.075 | 数值凑对，但仍为硬编码 |
| "年化复合超额超 19%" | 实际超额 25.51 pct | 不算错，但明显失准 |

**修复建议**: 评价列全部改为由 metrics 现算的表达式（倍数、笔/年、超额 pct 均可动态渲染）；基准 Sharpe/Calmar 加入 `benchmark_stats` 现算；策略描述中的均线窗口引用 `p['ma']`。

### 2.5 OOD 压力测试设计缺陷：白酒不是域外资产，测试近乎空转（P2）🟡

**位置**: `search_engine.py:213-223`、`universe.py:39-44`

- `domestic_pool` 本身包含 `512690`（酒ETF），而 `STRESS_UNIVERSE` 也含 `512690`——**"注入未训练的…白酒等异构资产"的说法不成立，白酒全程在训练池里**；真正域外的只有豆粕/养殖/钢铁 3 只；
- OOD 结果（Sharpe 0.923 / MDD 34.24% / 101 笔）与冠军本体（0.956 / 34.25% / 101 笔）几乎重合，说明混入的 3 只商品/周期 ETF 几乎未被选中——**该测试几乎没有提供增量信息**，"✅ 域外压力测试通过"的结论强度很低。

**修复建议**: 从训练池中移除 512690（或从 STRESS_UNIVERSE 移除），让"域外"名副其实；OOD 报告中注明压力资产实际被选中的天数（若为 0 天则如实说明测试无区分度）。

### 2.6 CPCV 的 Purge/Embargo 仍然名不副实（P2）🟡

**位置**: `cpcv_validator.py:37-46, 79-86`、`config.py:30-31`

- `split_dates_with_purging` 函数名带 "with_purging"，函数体仍是均匀切块，无任何净化逻辑；
- 实际的"净化"是砍掉每个测试块**头部 7 个自然日**（`purged_test_dates`），这只是丢弃少量测试样本，与 Lopez de Prado 语义下"切除训练标签与测试集重叠期"不是一回事；
- **`embargo_days` 仍是零引用的死参数**，注释写"尾部追加 embargo 保护"但没有任何对应代码；
- 本实现本质是"固定规则的分块 Walk-Forward + 测试块头部截断"。由于持仓矩阵是全因果预生成的（无逐折重训练），purge/embargo 在此框架下本来就不适用——问题在命名与注释误导，而非数值错误。

**修复建议**: 二选一——(a) 摘掉 CPCV/Purged 名号，如实命名 "Blocked Walk-Forward"，删除死参数；(b) 实现真正的逐折重训练 + purge/embargo（对当前固定规则策略而言成本高收益低，不推荐）。

### 2.7 胜率指标被货基利息日灌水（P3）🟡

**位置**: `backtester.py:177-178`

全样本 651 个 CASH 日中 636 天因 2% 现金利息产生 +0.0082% 的"正收益"，全部计入 `win_rate` 分子。实测胜率 67.85% 中约 30 个百分点的天数是纯利息日，胜率指标失真。

**修复建议**: 胜率仅在"有持仓"的交易日上统计，或剔除日收益绝对值 < 现金利息的天数。

### 2.8 次级问题（P3）

1. `downloader.py:56-77` 的特征工程（ma_/bias_/mom_/vol_/sharp_mom_ 系列）在当前策略中**全部未被使用**（策略直接由 close 现算），属死产物，徒增下载后处理时间；
2. `generate_domestic_sector_positions` 的 `all_syms` 恒定混入 `510880`（仅当 defense_mode 选它时才用），当前冠军配置下该列全零，属无效列；
3. `dsr_auditor` 对全样本收益（含盲测期）做通缩，混入了选拔后数据；建议 DSR 只用训练期收益（与选拔口径一致，更保守），或明确注释口径；
4. CPCV 子回测每条路径以全新 1 万元空仓起步（首日 `shift(1)` 补 0），路径初期建仓成本会轻微压低各路径夏普——方向保守，无害，建议注释说明；
5. `CPCVValidator.evaluate_positions` 中 `std_sharpe` 用 `np.std`（ddof=0），15 条路径建议 `ddof=1`；
6. 年度收益表按"年内首末净值比"计算，未衔接上年末净值（1 月初的损益不落在任何一年），与日历年度口径略有出入；建议用 `nav.resample('YE').last().pct_change()`。

---

## 三、第一轮 → 第二轮 修复确认（前朝问题销项）

| 第一轮问题（旧版） | 当前版本状态 |
|---|---|
| P0 同 bar 收盘偷价 | ✅ 已修复：`positions_df.shift(1)` T+1 撮合，行为级验证通过 |
| P0 DSR 不显著却宣称"审计通过" | ✅ 已修复：verdict 由 `is_significant_95/90` 动态渲染（本次如实输出 90% 宽容通过 / 95% 未通过） |
| P0 CPCV 闸门为死代码 | ✅ 已修复：真实闸门 + fallback 具名，冠军 CPCV 失败被如实展示（但见 2.1 总判定缺失） |
| P0 报告数字硬编码失实 | ⚠️ 大部分修复：正文数字全动态且复算一致；评价列/基准列仍残留 7 处硬编码，其中 5 处失实（见 2.4） |
| P0 CPCV double dipping（跨全样本） | ✅ 结构性修复：选拔限训练期，2025+ 盲测物理隔离；残余为训练期内选拔自引用（见 2.2） |
| P1 "Purged" 未实现 | ⚠️ 未修复：embargo 仍为零引用死参数，"purge"为测试块头部截断（见 2.6） |
| P1 QDII 后视资产对 | ✅ 已修复：剔除跨境 QDII，纯 A 股行业池（池层面后视残余见 2.3） |
| P1 sector 假 CPCV 参与排序 | ✅ 已修复：全家族统一走真实 CPCV |
| P2 TRAIN/TEST 配置未使用 | ✅ 已修复：两阶段物理划分生效 |
| P2 死代码（grammar/state_machine/双引擎 CPCV） | ✅ 已删除（两文件已移除，CPCV 统一走 validator） |

---

## 四、修复优先级建议（当前版本）

1. **P1-甲（结论诚实性）**: 报告顶部增加总判定——`is_cpcv_passed=false` 时明确"无策略达标、冠军为降级选拔"（2.1）。
2. **P1-乙（数字可信）**: 清掉报告残留的 7 处硬编码，评价列与基准列全部现算（2.4）。
3. **P1-丙（方法）**: CPCV 区块标注选拔自引用；资产池做敏感性测试；DSR 试验口径补入池自由度或明示局限（2.2 / 2.3）。
4. **P2**: OOD 池去重 + 白酒出池 + 报告区分度说明（2.5）；CPCV 更名或真实现（2.6）。
5. **P3**: 胜率口径、佣金下限、停牌成交注释、死特征清理、ddof、年度收益口径（2.7 / 2.8）。

---

## 五、审计声明

- 本轮审计全程只读：除本文件外未修改任何源码与产物；所有复算在内存中完成。
- 复算基于 `data/raw/*.parquet`（前复权日线，2018-01-02 ~ 2026-09-07）与当前版本 `src/` 代码；冠军全样本与盲测期指标均独立重现，产物与代码当前是对账一致的。
- "无前视/泄露"结论限于当前代码路径与当前数据；若更换资产池、参数或执行假设，需重新审计。

---

## 附录：第一轮审计结论存档（针对 16:37 前旧版 GEM 纳指/黄金版本，问题已随重构基本销项）

旧版五个 P0：①同 bar 收盘偷价；②DSR 0.8919 不显著但流水线无条件打印"审计通过"、报告宣称"极高统计显著性"；③CPCV 通过标准为死代码，冠军两项指标不达标未拦截；④报告历年收益/交易笔数/基准终值大面积失实（历年收益九个年份无一吻合、19 笔 vs 实际 31 笔、基准 11210 vs 实际 13347）；⑤CPCV 参与冠军选择后被当样本外证据（double dipping）。
P1：Purge/Embargo 纯装饰；sector 策略 CPCV 硬编码 0 参与排序（真实 min_oos 全负）；纳指+黄金资产对为后视选择；同 bar 撮合。
P2/P3：TRAIN/TEST 配置未使用；grammar.py 整体死代码；双夏普口径并存；停牌可成交；报告"新浪双通道"失实。
上述问题在当前版本中的状态见第三节销项表。
